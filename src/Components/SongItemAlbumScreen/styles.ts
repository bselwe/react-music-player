import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        borderTopWidth: 0,
        borderBottomWidth: 0
    },
    name: {
        padding: 5,
        fontSize: 20
    },
    id: {
        fontSize: 20,
        paddingLeft: 20,
        fontStyle: "italic",
        color: "white",
    }
});